To run the CSG Toolbox, just tap 'CSG' in the 'command window'.
For each function used, a message should be on your command window to invite you to cite authors if needed.

The CSG_Toolbox should be cited as:

CSG_Toolbox, Name_of_the_function, https://github.com/ComaScienceGroup.

Enjoy!